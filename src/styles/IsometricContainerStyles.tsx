import styled from "styled-components";

const IsometricContainerStyle = styled.div`
  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  overflow: hidden;
`;

export default IsometricContainerStyle;
